# FAQ / 常见问题

> 配套 [README.md](README.md) 与 [docs/findings/kv-cache-ttft.md](docs/findings/kv-cache-ttft.md)。
> 按主题分组：结果解读 · 方法论 · 缓存 · 质量 vs 速度 · 复现与对比。

---

## 结果解读

### Q: M5 Max 和 DGX Spark 综合分只差 1 分左右，到底谁更好？

分场景看，别看总分：

- **交互式科研（写代码、查文献、改稿）**：看 TTFT。DGX 升级后 0.62s、M5 Max 0.85s 都在"秒回"区，体感接近；但 M3 Max 6.12s 在"打断思路"区。
- **持续吞吐（长文生成、批量处理）**：看 tok/s。M5 Max 中位 ~42 t/s 明显高于 DGX 升级后 ~20-30 t/s。
- **首次喂新数据（冷 prefill）**：看 `prefill_tok_per_s`。M5 Max 实测 615 t/s；DGX 待用 v2 协议补测。
- **答案质量**：两者几乎无差（见下一条）。

README 里的建议：单人交互用 M5 Max（笔记本形态 + 吞吐高），团队共享 / 超长上下文用 DGX（1M ctx + 并发友好）。

### Q: DGX 质量分 76.9 比 M5 Max 73.8 高 3.1 分，说明 DGX 答题更准吗？

不能这么读。归因分析显示：8 个有评估器的任务里 **7 个得分完全相同**，全部 3.1 分差异来自
`expression_matrix` 一道题的 `top5_genes` 子项（M5 命中 3/5 基因、DGX 命中 5/5）。
而 `expression_matrix` 是我们自己定性为"设计翻车"的任务——LLM 做不了精确算术（README 已建议让它写 pandas）。
另外两个子项（`sparsity`、`top_cell`）在**两台机器上评估器都没提取到答案**（`got=None`），
说明差异里还混着评估器提取失败的可能。

20 题样本量下一道题 = 5%，把 3.1 分读成"DGX 答题更准"是过度解读。

### Q: 为什么 mutation_call 所有机器都是 0 分？是机器不行吗？

不是，这是**任务设计翻车**，我们故意保留它作为诚实的边界标记：
LLM 做不了 7000+ nt 序列的字符级逐位对齐（每条序列 ~2000 token，逐位比较超出当前模型能力）。
正确做法是把序列交给 BLAST/BWA/minimap2，让 LLM 只解读比对结果。
所有机器 P=0 R=0 恰恰说明这是**模型类别的共同短板**，不是某台机器的问题。

### Q: M5 Max 两次跑分差了 0.8 分（74.0 vs 73.2），机器不稳定吗？

不是。除 TTFT 外所有指标两天间完全一致（吞吐 42 vs 42 t/s，质量 73.8 vs 73.8）。
差异全部来自 TTFT 子分（59.7 vs 55.4），根因是 **KV 前缀缓存冷热**：
第一次跑时服务器已热（缓存命中，pubmedqa TTFT 2.6s），第二次是冷启动（全量 prefill，11.8s）。
v2 协议已修复（cold/hot 分报），见下面缓存相关 FAQ。

### Q: "2 台 DGX，按 token 输出，DGX 方案感觉会快点"——对吗？

**单请求（单流 decode）不成立，多用户并发场景才成立。** 分开看：

**单流 decode 实测（最干净的指标，`long_generation`：短 prompt 长输出，prefill 占比 <3%）：**

| 方案 | tok/s |
|------|-------|
| M5 Max 单机（ds4-server，q2）| **41.7** |
| DGX 2 节点 TP=2（vLLM 升级后）| 22.9 |

单流 M5 快 **1.83×**。短任务 decode 侧同样落后（expression_genes 43 vs 25、protein_function 43 vs 21）。

**为什么 TP=2 不加速单请求 decode？** decode 是 memory-bound：每生成一个 token 都要读一遍全部权重，
瓶颈是显存/统一内存带宽，不是算力。TP=2 把权重切到两台机器、每台只读一半，
但每个 token 都要跨 InfiniBag/以太网同步 activation——通信延迟吃掉带宽收益，
单请求通常持平甚至更慢。TP 加速的是**大 batch**（一次前向算 16/32 个请求，
通信被摊薄）和**大模型单机放不下**的场景。

**逐任务表里 DGX 反超的项别误读。** 比如 pubmedqa tok/s M5 17.7 vs DGX 31.0（×1.75）——
那不是 decode 快，是 DGX 的 NVFP4 KV cache 把 prefill 压到 0.76s，摊薄了 total。
`tok/s = tokens / total_s`，total 里混着 prefill，跨机器 prefill 差 10 倍时这个数不可比。
纯 decode 只看 `long_generation`。

**那"感觉快"从哪来？** 三个真实来源：
1. **TTFT 秒回**：DGX 升级后 0.62s 首字，交互体感"快"
2. **DSpark 投机解码**：贪婪模式下某些任务吞吐 +36%
3. **并发总吞吐**：这是 2×DGX 的真正价值——两条路：
   - **独立部署**（不用 TP）：两台各自 serve，总吞吐 2×22.9 = 45.8 t/s ≈ M5 单机 41.7，但能同时服务两个用户
   - **TP=2 + continuous batching**：多用户并发时总吞吐远超单机（batch 越大通信摊得越薄）

**本 benchmark 的协议边界**：单请求顺序发送，天然测不出并发优势——这是协议设计
（科研场景以单用户为主），不是 DGX 不行。要测并发需要多客户端变体（Roadmap 里有）。
**选型结论**：单人用选 M5（单流快 1.8×、笔记本）；多人共享/批量服务选 2×DGX
（并发吞吐和 1M 上下文是 Mac 给不了的）。

---

## 方法论

### Q: 综合分怎么算的？为什么质量占 50%？

```
总分 = 质量×50% + 吞吐×30% + TTFT×10% + 稳定性×10%
```

生物医学研究里**答错比慢更严重**（一个错的变异注释可能误导整个分析方向），
所以质量权重最高。吞吐第二（决定实际科研效率）。TTFT 和稳定性影响体感但不影响正确性，权重低。
每个子项独立归一到 0-100（对数锚点），单机得分有意义，不依赖其他机器。

注意：README 的 UX 结论提醒——如果你主要做交互式科研，建议在脑子里把 TTFT 权重往上抬一档。

### Q: TTFT、tok/s 这些数字受缓存影响吗？

受，而且影响很大（见下一节）。v2 协议后请这样读：

| 指标 | 含义 |
|------|------|
| `cold_ttft_s` | 本 benchmark 进程内首发（可能仍命中跨进程磁盘缓存）|
| `ttft_s` 均值 | 混合负载（综合分用这个）|
| `hot_ttft_s` | 缓存必然最热（迭代场景体感）|
| `prefill_tok_per_s` | nonce 强制 miss，**跨引擎公平的纯 prefill** |

### Q: 为什么用 20 题 PubMedQA/MedMCQA？够吗？

不够做精细比较，够做机器选型。20 题下一题 = 5%，能稳定区分"差 20 分"级别的差距
（比如 M3 Max vs M5 Max），区分不了 1-3% 的精细差异（比如不同量化的影响）。
想分辨后者：扩到 `pqa_labeled` 全集（1000 题）+ 多 seed，见"量化"一节的 FAQ。

### Q: 跨机器对比前要注意什么？

1. **对齐服务器缓存状态**：都冷启动，或都充分预热（最重要）
2. **同 RUNS**（默认 5；`RUNS=1` 只适合冒烟）
3. **安静机器**：关浏览器、无其他 GPU 任务
4. **别中断首次预热**：冷 GPU kernel 编译可能要 30-60s，不是卡死
5. 分享 **JSON 而非只发 PNG**：JSON 有完整 per-run 样本，可重放分析

---

## KV 缓存

### Q: 什么是"TTFT 被缓存污染"？

ds4-server（磁盘 KV 缓存）和 vLLM（automatic prefix caching）都会缓存 prompt 前缀。
同一 prompt 重复发送时 TTFT 骤降——实测 M5 Max：

```
expression_matrix:  cold 1.75s → hot 0.05s   (×33)
protein_function:   cold 2.46s → hot 0.05s   (×46)
pubmedqa:           cold 11.8s → hot 2.5s   (×4.6)
```

缓存红利是引擎的真实能力（科研迭代场景确实受益），但如果不区分冷热，
TTFT 既不代表"首次喂新数据"也不代表"日常迭代"，跨引擎对比也随之失真。

### Q: `prefill_nonce` 任务是怎么绕过缓存的？

每个 run 在 33KB prompt 开头埋一个唯一 nonce（`Random(9582019 + run_idx)`，确定性、跨机器一致）。
前缀缓存按前缀匹配，头部不同整条即 miss → 每发都是完整 prefill → TTFT 就是纯 prefill 算力。
M5 Max 实测 5 发全部 ~12.0s（CV<2%），换算 615 tok/s。

### Q: DGX 升级后 pubmedqa TTFT 0.76s 是真算力还是缓存命中？

**目前无法从数据里分辨**，这正是 v2 协议要补的洞。0.76s × 9700 tok/s 的 prefill 速度
在 GB10 上存疑，且升级前那轮 `[1.19, 41.99]` 的 35 倍内部差异就是缓存签名的直接证据
（1.19s 是 warmup 写入的缓存命中，41.99s 是全量 prefill）。
需要 DGX 用 v2 协议重跑 + 导出 vLLM prefix-cache 命中率来定论。

---

## 质量 vs 速度（含量化）

### Q: DGX 的优势是"原生支持 NVFP4，量化精度高损失小"——测试体现出来了吗？

**没有体现，而且这个说法本身混了两个概念：**

1. **概念澄清**：DGX 上跑的模型是官方 0731 权重（原生 MXFP4 experts，4-bit）；
   NVFP4 在 DGX recipe 里是 **KV cache 格式**（DS-MLA），不是权重量化。
   GB10 的 FP4 tensor core 原生跑这两者——那是算力/带宽优势的来源，跟"量化精度"是两回事。

2. **数据归因**：M5（社区 q2 重量化）vs DGX（官方 4-bit）质量差 3.1 分，
   100% 来自一道"设计翻车"算术题的一个子项（见上面结果解读 Q2）。
   6 个任务上 2-bit 和官方 4-bit 给出**一模一样**的答案——这反而侧面验证了
   ds4 非对称量化（routed experts 压 2-bit、attention 保 Q8）的损失控制。

3. **NVFP4 真正兑现的是速度**：DGX 升级前后（同机同引擎，最接近控制变量的对比）
   吞吐 +36%、pubmedqa TTFT 21.59s → 0.76s——README 也写明修好长 prefill 的就是
   NVFP4 KV cache。FP4 的价值主张就是带宽减半，我们的数据支持的正是这个。

**想真正验证"量化损失"需要专门实验**：同机同引擎关掉 NVFP4/DSpark 各跑一轮（控制变量）；
题库扩到 500+ 题 + 多 seed（统计功效）；或用 ds4 自带的 `--dump-logprobs` /
`gguf-tools/quality-testing/` 做 logit 级对照（金标准）。当前 benchmark 是机器选型工具，
不是量化损失检测器。

### Q: 那 2-bit 量化到底有没有质量损失？我的 M5 Max 答案可信吗？

在**这套测试覆盖的任务类型**上（知识问答、代码生成、结构化输出、长文写作），
q2 与官方权重答案一致。但这不等于"全面无损"：

- 测试没覆盖：复杂多步推理、低频领域知识、边界指令遵循——量化损失往往藏在这些地方
- 20 题样本量看不见 1-3% 级别的差异
- 金标准是 ds4 的官方 logits 对照（test-vectors），q2 的 perplexity/延续一致率见 ds4 仓库

实用建议：日常科研（生成分析代码、写综述、查文献）q2 足够可信；
关键决策（临床相关的精细判断）建议用官方权重引擎复核。

### Q: 投机解码（DSpark）会影响答案质量吗？

理论上贪婪模式下投机解码**不改变输出**（验证器保证与主模型 greedy 一致），
这也是 ds4 把它做成 correctness-gated 的原因。但注意：
- 我们的测试跑的是 temperature=0，投机解码的安全性前提就是贪婪
- DGX 升级后质量分 +1.1 在 20 题样本下是噪声量级，既不能证明也无损证据

---

## 复现与对比

### Q: 我怎么提交自己机器的结果？

见 README 贡献指南（`results/community/`）。最小交付：

```bash
# 跑完发这两个文件
results/bench_<你的label>_<时间戳>.json
results/bench_<你的label>_<时间戳>.png
```

要求：RUNS=5、冷启动或预热后说明状态、附引擎版本和量化信息。

### Q: test_dgx.py 和已删除的 test_mac.py 是什么关系？

`test_mac.py` 曾是 ds4 CLI 直调的单任务变体，后来删除（价值窄、文档易腐）。
历史结论保留在 git log。现在所有测试统一走 HTTP API（`test_dgx.py`），
任何 OpenAI 兼容引擎都能测。

### Q: 综合分里的 TTFT 权重为什么只有 10%？我觉得体感更重要

10% 是为了兼顾批量场景。README 的 UX 结论给了更实用的读法：
交互场景按 <1s / 1-3s / >5s 三档读 TTFT，别看加权分。
如果社区共识认为交互场景更重要，权重可以调（`SCORE_WEIGHTS` 一行），
但调整后所有历史分数需要重放更新。

### Q: 跑分时首次很慢（30-60s+），是卡死了吗？

不是。冷 GPU 首次推理触发 kernel JIT 编译（尤其 Metal/CUDA），预热就是干这个的。
正式 RUNS 不含 warmup，统计不受影响。别中断。

### Q: 为什么我的 TTFT 和别人差很多，吞吐却一样？

先查缓存状态（`cache_speedup_ttft` 字段）：你的服务器是不是刚启动（冷），
对方是不是跑了好多轮（热）？对齐状态后重跑再比。其次查 `prefill_nonce` 任务的
TTFT——那是无缓存污染的公平数字。

---

## 维护者备忘

- 评估器正则对**代码风格敏感**（换行排版、新旧 API），已修复两处
  （跨行 `read_csv`、`sc.pp.pca`），但同类风险仍在——新增评估器时优先宽松匹配 + 人工抽查
- `expression_matrix` 的 `sparsity`/`top_cell` 提取在两台机器上都失败过（`got=None`），
  待修；修复后历史 JSON 需重放
- DGX 的 `prefill_tok_per_s` 待补（v2 协议重跑后填入排行表）
- 20 题样本的统计局限是已知设计取舍；扩题库脚本在 `test_data/` 的生成脚本历史里
